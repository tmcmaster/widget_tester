name: widget_tester
description: A new Flutter package project.
version: 0.0.1
homepage: https://github.com/tmcmaster/widget_tester/blob/main/README.md

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.2.0"

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod:
  faker: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic:
  lint:
  test:
  flutter_workbench:
    path: ../flutter_workbench
  theme_builder:
    path: ../theme_builder